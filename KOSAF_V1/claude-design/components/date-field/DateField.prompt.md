Date / time entry using the NATIVE picker, styled 287×50 r10 (lg) or 40 r5 (sm) — use it for forms on KOSAF PC (1920) and mobile (390–391) screens.

```jsx
<DateField aria-label="발주기한" defaultValue="2022-03-23"/>
```

- Provenance: **Source-derived (1:85779)** · node 1:94245 Calender. Structure, sizes and colours follow the exported source; behaviour beyond what the screen shows is a KOSAF extension.
- States: Default · Focus · Error · Disabled.
- Props: `value`, `onChange`, `type`, `width`, `size`, `min`, `max`, `disabled`, `error`.
- Date / time entry using the NATIVE picker, styled 287×50 r10 (lg) or 40 r5 (sm). Custom calendar popover: not built / unverified.

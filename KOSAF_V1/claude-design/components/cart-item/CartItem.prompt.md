장바구니/주문 line: select, thumb, specs, 주문수량, * 납품기일, * 납품장소 + 배송지 변경, 운임. — use it for commerce on KOSAF PC (1920) and mobile (390–391) screens.

```jsx
<CartItem imageSrc="assets/products/product-mobile__1-91522.png" quantity={q} onQuantity={s} deliveryDate="2022-03-23" selectable selected={c} onSelect={sc} onRemove={()=>{}}/>
```

- Provenance: **Source-derived (1:85779)** · node 1:103945 · 1:104139. Structure, sizes and colours follow the exported source; behaviour beyond what the screen shows is a KOSAF extension.
- States: Selectable · Selected · Read-only qty.
- Props: `imageSrc`, `title`, `price`, `specs`, `quantity`, `onQuantity`, `editableQuantity`, `deliveryDate`, `place`, `onChangePlace`, `freight`, `selectable`, `selected`, `onSelect`, `onRemove`, `device`.
- 장바구니/주문 line: select, thumb, specs, 주문수량, * 납품기일, * 납품장소 + 배송지 변경, 운임.

Page navigation with first/prev/next/last; PC uses PaginationItem; mobile compact (5 pages). — use it for navigation on KOSAF PC (1920) and mobile (390–391) screens.

```jsx
<Pagination total={23}/>
```

- Provenance: **Source-derived (1:85779)** · node 1:97566 / 1:90254. Structure, sizes and colours follow the exported source; behaviour beyond what the screen shows is a KOSAF extension.
- States: Current · First/last disabled.
- Props: `page`, `total`, `window`, `onChange`, `device`.
- Page navigation with first/prev/next/last; PC uses PaginationItem; mobile compact (5 pages).

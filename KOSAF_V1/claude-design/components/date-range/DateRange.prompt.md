Start ~ end dates; end is constrained by start (native min/max). — use it for forms on KOSAF PC (1920) and mobile (390–391) screens.

```jsx
<DateRange defaultStart="2023-03-01" defaultEnd="2023-03-23"/>
```

- Provenance: **Source-derived (1:85779)** · node 1:85780 ("~"). Structure, sizes and colours follow the exported source; behaviour beyond what the screen shows is a KOSAF extension.
- States: Default · Disabled.
- Props: `start`, `end`, `onChange`, `width`, `size`, `disabled`.
- Start ~ end dates; end is constrained by start (native min/max).

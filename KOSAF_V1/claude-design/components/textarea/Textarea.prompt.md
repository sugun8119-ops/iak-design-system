Long text (후기/문의) — use it for forms on KOSAF PC (1920) and mobile (390–391) screens.

```jsx
<Textarea aria-label="후기" width={660}/>
```

- Provenance: **Source-derived (1:85779)** · node 1:87378. Structure, sizes and colours follow the exported source; behaviour beyond what the screen shows is a KOSAF extension.
- States: Default · Focus · Error · Disabled.
- Props: `value`, `onChange`, `placeholder`, `maxLength`, `height`, `size`, `disabled`, `error`.
- Long text (후기/문의). 925×204 r5, 22/34, #CCCCCC placeholder, live counter "0/300".

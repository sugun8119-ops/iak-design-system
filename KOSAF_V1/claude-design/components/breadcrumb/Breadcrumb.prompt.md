Location path — use it for navigation on KOSAF PC (1920) and mobile (390–391) screens.

```jsx
<Breadcrumb items={['홈','마이페이지','주문관리','주문/배송내역']}/>
```

- Provenance: **KOSAF extension** · node text "A > B". Not a Figma master — styling/behaviour defined by KOSAF_V1.
- States: link · current.
- Props: `items`, `onNavigate`, `size`.
- Location path. Built from the "과실류 > 사과" / "마이페이지 > 주문/배송내역" text pattern; no Figma component.

390px top bar: menu (CSS bars) · centred title · search + cart (source icons). — use it for navigation on KOSAF PC (1920) and mobile (390–391) screens.

```jsx
<MobileHeader cartCount={3}/>
```

- Provenance: **Source-derived (1:85779)** · node 1:87784. Structure, sizes and colours follow the exported source; behaviour beyond what the screen shows is a KOSAF extension.
- States: menu · back.
- Props: `title`, `logoSrc`, `onMenu`, `onSearch`, `onCart`, `cartCount`, `back`, `onBack`.
- 390px top bar: menu (CSS bars) · centred title · search + cart (source icons).

− value + quantity control; ↑/↓ keys; clamps to min/max; typed numbers. — use it for forms on KOSAF PC (1920) and mobile (390–391) screens.

```jsx
<QuantityStepper defaultValue={100}/>
```

- Provenance: **Source-derived (1:85779)** · node Spinner sample / 1:87593. Structure, sizes and colours follow the exported source; behaviour beyond what the screen shows is a KOSAF extension.
- States: Default · At min/max (button disabled) · Disabled.
- Props: `value`, `onChange`, `min`, `max`, `step`, `size`, `disabled`.
- − value + quantity control; ↑/↓ keys; clamps to min/max; typed numbers.

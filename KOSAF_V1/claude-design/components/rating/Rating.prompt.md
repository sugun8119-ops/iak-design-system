Star score — use it for data display on KOSAF PC (1920) and mobile (390–391) screens.

```jsx
<Rating value={4.4} showValue size={22}/>
```

- Provenance: **KOSAF extension** · node ★ glyph. Not a Figma master — styling/behaviour defined by KOSAF_V1.
- States: Display (fractional) · Input · Hover.
- Props: `value`, `onChange`, `max`, `size`, `showValue`, `readOnly`.
- Star score. Star vectors not exported → ★ text glyph in #FFE326. Display or input (radio group, ←/→).

Toggle pill for 판매자 유형/거래방식/도매시장 and removable applied-filter tag ("사과 ×"). — use it for actions on KOSAF PC (1920) and mobile (390–391) screens.

```jsx
<FilterChip />
```

- Provenance: **Source-derived (1:85779)** · node 1:97566 (capture). Structure, sizes and colours follow the exported source; behaviour beyond what the screen shows is a KOSAF extension.
- States: Default · Selected · Disabled · Removable.
- Props: `selected`, `onToggle`, `variant`, `onRemove`, `disabled`, `children`.
- Toggle pill for 판매자 유형/거래방식/도매시장 and removable applied-filter tag ("사과 ×").

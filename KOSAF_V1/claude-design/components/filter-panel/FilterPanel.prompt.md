통합검색 filters — use it for commerce on KOSAF PC (1920) and mobile (390–391) screens.

```jsx
<FilterPanel defaultValue={{sort:['농산물'],class:['사과','포도'],seller:['위탁']}} keyword="사과" onlyOnSale onClose={()=>{}}/>
```

- Provenance: **Source-derived (1:85779)** · node 1:97566 · 1:93119. Structure, sizes and colours follow the exported source; behaviour beyond what the screen shows is a KOSAF extension.
- States: Option selected · Section open (mobile) · Applied tags.
- Props: `device`, `groups`, `value`, `onChange`, `keyword`, `onSearch`, `onClose`, `onlyOnSale`.
- 통합검색 filters. PC: green label column + 4 category lists + chip rows + 전체해제/applied tags + 닫기. Mobile: accordion checkbox lists + 초기화.

전체메뉴 drawer: accordion sections (open list on #F7F7F7), 입점 판매샵 목록, 4-col quick menu, bottom links — use it for navigation on KOSAF PC (1920) and mobile (390–391) screens.

```jsx
<MobileMenu/>
```

- Provenance: **Source-derived (1:85779)** · node 1:93205. Structure, sizes and colours follow the exported source; behaviour beyond what the screen shows is a KOSAF extension.
- States: Section open · closed.
- Props: `sections`, `quick`, `defaultOpen`, `onClose`, `onSelect`, `bottomLinks`.
- 전체메뉴 drawer: accordion sections (open list on #F7F7F7), 입점 판매샵 목록, 4-col quick menu, bottom links. Esc closes.

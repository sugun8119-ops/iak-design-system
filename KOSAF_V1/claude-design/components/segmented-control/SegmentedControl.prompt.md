Period / view toggle (기간 선택) — use it for forms on KOSAF PC (1920) and mobile (390–391) screens.

```jsx
<SegmentedControl/>
```

- Provenance: **KOSAF extension** · node Common_진행중 Toggle sample. Not a Figma master — styling/behaviour defined by KOSAF_V1.
- States: Default · Selected (fill + Bold) · Focus.
- Props: `options`, `value`, `onChange`, `height`.
- Period / view toggle (기간 선택). Source sample exists but geometry was not exported → styling is a KOSAF extension.

document.querySelectorAll('[data-demo-tabs]').forEach(group=>group.querySelectorAll('button').forEach(button=>button.addEventListener('click',()=>{group.querySelectorAll('button').forEach(b=>b.setAttribute('aria-pressed',String(b===button)))})));
document.querySelectorAll('[data-demo-toggle]').forEach(button=>button.addEventListener('click',()=>{const pressed=button.getAttribute('aria-pressed')!=='true';button.setAttribute('aria-pressed',String(pressed));button.textContent=pressed?button.dataset.on:button.dataset.off;const status=button.parentElement.querySelector('[data-demo-status]');if(status)status.textContent=pressed?'표본의 저장 상태가 켜졌어요.':'표본의 저장 상태가 해제됐어요.'}));
const toast=document.querySelector('.toast-sample'),show=document.querySelector('[data-show-toast]');document.querySelector('[data-dismiss-toast]')?.addEventListener('click',()=>{toast.hidden=true;show.focus()});show?.addEventListener('click',()=>{toast.hidden=false});

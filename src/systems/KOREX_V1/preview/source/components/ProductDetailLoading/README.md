# ProductDetail — loading

ProductDetail 템플릿의 `loading` 상태 전체 화면. 컴포넌트와 페이지 셸은 기본 ProductDetail와 같고, 상태 표현만 바뀐다.

- 로딩: 레이아웃과 같은 크기의 Skeleton, `aria-busy`, 관련 컨트롤은 비활성(Pagination disabled, 버튼 disabled). **derived-extension**.

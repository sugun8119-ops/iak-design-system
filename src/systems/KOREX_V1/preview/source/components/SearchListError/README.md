# SearchList — error

SearchList 템플릿의 `error` 상태 전체 화면. 컴포넌트와 페이지 셸은 기본 SearchList와 같고, 상태 표현만 바뀐다.

- 오류: 인라인 오류 Toast(잉크 면 + × + “오류” 단어 + 해결 문장 + “다시 시도”). 나머지 페이지(헤더·검색·브랜드 영역)는 그대로 쓸 수 있다. 실제 API 호출은 없다. **derived-extension**.

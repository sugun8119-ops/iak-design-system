# BrandHome — missing-image

BrandHome 템플릿의 `missing-image` 상태 전체 화면. 컴포넌트와 페이지 셸은 기본 BrandHome와 같고, 상태 표현만 바뀐다.

- 이미지 누락: 점선 틀 + 이미지 없음 아이콘 + “이미지 준비 중”(대체 텍스트 “이미지 없음”). 레이아웃 비율(4:3 / 3:4)은 유지한다. **derived-extension**.

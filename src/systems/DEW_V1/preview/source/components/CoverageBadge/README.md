# CoverageBadge

All 10 IAK Badge cases rendered with the DEW `Badge` implementation (or the named composition / native attribute / design-only visual sample). Anchors: `case-badge-<id>`. Status: **derived-extension**.

# CoverageSkeleton

All 4 IAK Skeleton cases rendered with the DEW `Skeleton` implementation (or the named composition / native attribute / design-only visual sample). Anchors: `case-skeleton-<id>`. Status: **derived-extension**.

# CoverageTextarea

All 9 IAK Textarea cases rendered with the DEW `Textarea` implementation (or the named composition / native attribute / design-only visual sample). Anchors: `case-textarea-<id>`. Status: **derived-extension**.

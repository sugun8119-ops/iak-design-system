# CoverageToast

All 11 IAK Toast cases rendered with the DEW `Toast` implementation (or the named composition / native attribute / design-only visual sample). Anchors: `case-toast-<id>`. Status: **derived-extension**.

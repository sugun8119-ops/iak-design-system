# CoverageMenu

All 6 IAK Menu cases rendered with the DEW `Menu` implementation (or the named composition / native attribute / design-only visual sample). Anchors: `case-menu-<id>`. Status: **derived-extension**.

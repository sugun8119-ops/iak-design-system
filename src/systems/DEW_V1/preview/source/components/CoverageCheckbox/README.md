# CoverageCheckbox

All 7 IAK Checkbox cases rendered with the DEW `Checkbox` implementation (or the named composition / native attribute / design-only visual sample). Anchors: `case-checkbox-<id>`. Status: **derived-extension**.

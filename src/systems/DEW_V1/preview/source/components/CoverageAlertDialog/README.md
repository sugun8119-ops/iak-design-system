# CoverageAlertDialog

All 6 IAK AlertDialog cases rendered with the DEW `AlertDialog` implementation (or the named composition / native attribute / design-only visual sample). Anchors: `case-alertdialog-<id>`. Status: **derived-extension**.

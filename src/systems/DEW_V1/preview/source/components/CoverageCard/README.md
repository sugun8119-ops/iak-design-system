# CoverageCard

All 6 IAK Card cases rendered with the DEW `Card` implementation (or the named composition / native attribute / design-only visual sample). Anchors: `case-card-<id>`. Status: **derived-extension**.

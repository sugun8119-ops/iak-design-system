# CoverageTable

All 11 IAK Table cases rendered with the DEW `Table` implementation (or the named composition / native attribute / design-only visual sample). Anchors: `case-table-<id>`. Status: **derived-extension**.

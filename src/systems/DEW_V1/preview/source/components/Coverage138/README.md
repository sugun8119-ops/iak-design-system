# Coverage138

Index of the 138 IAK coverage cases (16 families). Every case renders in its family card `Coverage<Family>` with the stable anchor `case-<family>-<id>`; the machine-readable list is `preview/case-coverage.json` in the download. Status: **derived-extension** (IAK = coverage only, DEW = visual authority).

# CoverageDialog

All 7 IAK Dialog cases rendered with the DEW `Dialog` implementation (or the named composition / native attribute / design-only visual sample). Anchors: `case-dialog-<id>`. Status: **derived-extension**.

# CoverageIcon

All 7 IAK Icon cases rendered with the DEW `Icon` implementation (or the named composition / native attribute / design-only visual sample). Anchors: `case-icon-<id>`. Status: **derived-extension**.

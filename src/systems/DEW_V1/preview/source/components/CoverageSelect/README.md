# CoverageSelect

All 8 IAK Select cases rendered with the DEW `Select` implementation (or the named composition / native attribute / design-only visual sample). Anchors: `case-select-<id>`. Status: **derived-extension**.

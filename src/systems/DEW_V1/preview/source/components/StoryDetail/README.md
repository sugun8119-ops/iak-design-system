# StoryDetail

Detail template: **EditorialHeader → ArticleMeta + `display` title + lead → MediaCard (Landscape, full width) → body column (max 720px, centered) → QuoteBlock → back EditorialCTA → Footer**.

The narrow body column against the full-width image is the page's asymmetry; paragraphs sit 24 apart and sub-heads (`heading`) 48 above.

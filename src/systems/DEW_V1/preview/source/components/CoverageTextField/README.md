# CoverageTextField

All 13 IAK TextField cases rendered with the DEW `TextField` implementation (or the named composition / native attribute / design-only visual sample). Anchors: `case-textfield-<id>`. Status: **derived-extension**.

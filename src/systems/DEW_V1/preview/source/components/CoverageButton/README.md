# CoverageButton

All 19 IAK Button cases rendered with the DEW `Button` implementation (or the named composition / native attribute / design-only visual sample). Anchors: `case-button-<id>`. Status: **derived-extension**.

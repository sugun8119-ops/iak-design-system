# CoverageSwitch

All 6 IAK Switch cases rendered with the DEW `Switch` implementation (or the named composition / native attribute / design-only visual sample). Anchors: `case-switch-<id>`. Status: **derived-extension**.

# CoveragePagination

All 8 IAK Pagination cases rendered with the DEW `Pagination` implementation (or the named composition / native attribute / design-only visual sample). Anchors: `case-pagination-<id>`. Status: **derived-extension**.
